#include<stdio.h>
int main()
{
char str[50];

puts("Enter any string"); // using for output only for string
gets(str); // using for input only for string

puts(str);// using for output only for string
return 0;
}